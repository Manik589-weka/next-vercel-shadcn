# Sanity CMS – required for blog and Studio
# Get project ID and dataset from https://www.sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production

# Optional: for draft/preview (server-side only, do not expose to client)
# SANITY_API_READ_TOKEN=your_token_here

# Optional: Sanity API version (defaults to 2024-01-01 if unset)
# NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01
